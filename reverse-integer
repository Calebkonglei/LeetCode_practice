/**
 * @param {number} x
 * @return {number}
 */
var reverse = function(x) {
    var num=parseInt(x.toString().split("").reverse().join(""));
    if(num>(Math.pow(2,31)-1)) return 0;//如果超出整形范围，返回0
    return x>0?num:0-num;
};
